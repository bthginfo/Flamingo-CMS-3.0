# Bugs und Risiken

Priorität folgt dem Brief: P0 kritisch, P1 hoch, P2 mittel, P3 niedrig. „Bestätigt“ bedeutet reproduziert oder direkt durch einen ausführbaren Codepfad bewiesen; „Hohe Sicherheit“ benötigt noch eine kontrollierte Laufzeitbedingung.

## P0 — Kritisch

| ID | Confidence | Kategorie | Befund | Betroffene Stellen | Reproduktion/Evidence | Auswirkung | Lösung / Migration | Regressionstest |
|---|---|---|---|---|---|---|---|---|
| F-001 | Hohe Sicherheit | Security/E-Mail | Öffentliche Flamingo-Mail-Relay-API | `apps/marketing/src/app/api/send-lead-email/route.ts:51-104`; `apps/marketing/src/middleware.ts:42-67` | Die Route nimmt ohne Session beliebige `to`, `subject`, `body` und bis 8 MB Attachment an und übergibt sie an Plattform-SMTP. Middleware matcht nur `/crm/:path*` und lässt API-Pfade ausdrücklich durch. Erreichbarkeit ist im Code belegt; tatsächlicher Versand setzt konfigurierte Production-SMTP-Variablen voraus und wurde aus Sicherheitsgründen nicht praktisch getestet. | Bei aktivem SMTP: Spam/Phishing über vertrauenswürdige Domain, Kosten, Reputations-/Blocklist-Schaden, Attachment-Missbrauch. | Sofort Handler/shared server verifier mit CRM-Session/Rolle verlangen oder Endpunkt deaktivieren; Middleware allein reicht wegen API-Bypass nicht. Empfänger/Template/Subject aus serverseitigem Lead-Datensatz; Rate-Limit, Größen-/MIME-Gate, Audit-Log. | Unauth POST→401; falsche Rolle→403; Empfänger-Manipulation ignoriert; erlaubter CRM-Flow sendet genau einmal. |
| F-002 | Bestätigt | Security/Data integrity | Öffentliche Demo-JWTs dürfen irreversible Mutationen ausführen | `admin/demo-login/route.ts:39-129`, `lib/session.ts`, `admin/media-actions.ts:10-14,213-232`, `pages/actions.ts:17-21`, `settings-actions.ts:11-15`, `collections/actions.ts` | `/admin/demo-login?industry=handwerk` mintet Rolle `demo`. Viele Actions prüfen nur `getSession()`. `deleteMediaAsset` kann den physischen Blob löschen; Seiten/Sections/Settings/Collections werden ebenfalls persistiert. `getWritableSession()` existiert, wird aber nicht durchgängig verwendet; Publish und Upload sind bereits gesperrt. | Jeder Besucher kann gemeinsame Demo-Daten verändern/löschen und durch Blob-Löschung den öffentlichen Showcase brechen. Das ist reale Datenzerstörung, obwohl Publish blockiert ist. | Vorhandenes `getWritableSession()` zu einer default-deny Capability-Policy erweitern und **jede** Servermutation migrieren. Kurzfristig Demo vollständig read-only; mittelfristig pro Session geklonter, automatisch gelöschter Sandbox-Tenant oder reine lokale Preview-Drafts. Seed-Restore allein ist kein Auth-Ersatz. | Automatisiertes Mutationsinventar: Demo erhält für jeden DB-/Blob-/Mail-/Secret-Write 403, Admin bleibt erlaubt; DB-/Provider-Diff nach kompletter Demo-Journey ist leer; Sandbox-Isolationstest mit zwei Sessions. |
| F-034 | Bestätigt | Secret exposure/Operations | Im Task-Verlauf geteilter Vercel-PAT gilt als kompromittiert | Externer Task-Kontext; bewusst nicht in Dateien, Logs oder Berichtswert übernommen | Ein Zugriffstoken wurde im Klartext in die Unterhaltung gestellt. `audit:credentials` prüft nur das Git-Repository und kann diese Exposition nicht entkräften. | Abhängig vom Scope: Zugriff auf Projekte, Deployments, Domains oder Environment Secrets. | **Sofort widerrufen**, Vercel-Aktivität seit Exposition prüfen, nur bei Bedarf ein neues least-privilege/kurzlebiges Token erzeugen und alle möglicherweise lesbaren Folge-Secrets bewerten/rotieren. | Altes Token erhält 401; Audit-Log geprüft und dokumentiert; neues Token nie in Chat/Repo/Logs. |

## P1 — Hoch

| ID | Confidence | Kategorie | Befund | Betroffene Stellen | Reproduktion/Evidence | Auswirkung | Lösung / Migration | Regressionstest |
|---|---|---|---|---|---|---|---|---|
| F-003 | Hohe Sicherheit | Publishing/Privacy | Fehler beim Snapshot-Lesen legt Draft öffentlich frei | `apps/renderer/src/lib/snapshot.ts:getActiveSnapshotMeta`, `getActiveSnapshot` | `getActiveSnapshotMeta` fängt jeden DB-/Queryfehler und liefert `null`; `getActiveSnapshot` interpretiert dies wie „nie publiziert“ und liest Draft-Tabellen. Kontrollierte Bedingung: Snapshot-Tabelle/Query gestört, Page-Tabellen erreichbar. | Unveröffentlichte Inhalte, Preise, Entwürfe oder personenbezogene Testdaten können öffentlich erscheinen. | Ergebnis als `active`, `never-published` oder `error` modellieren; bei `error` letzten bekannten Snapshot/503, niemals Draft. Legacy-Fallback nur über expliziten Tenant-Migrationsstatus. | Snapshot-Query wirft → Public liefert 503/letzten Snapshot; „never published“ darf nur explizit Draft/Setup-Page nutzen. |
| F-004 | Bestätigt | Core Admin | Reale Admin-Seitenliste crasht als Server Component | Live `/admin/pages` nach `/admin/demo-login?industry=handwerk`; `admin/error.tsx` | Reproduzierbar, auch nach „Erneut versuchen“; Digest `1429267296`. [Screenshot](evidence/admin-pages-rsc-error.png). | Kernjourney „Seite auswählen/erstellen“ blockiert; Demo und Kaufentscheidung beschädigt. | Vercel-RSC-Logs mit Digest korrelieren, Tenant-Daten/Query isolieren, Fehlerzustand mit Support-ID; Smoke-Test gegen seeded Demo. | Login→`/admin/pages` zeigt Seitentabelle, kein `pageerror`/RSC-Digest; Retry-Test. |
| F-005 | Bestätigt | Publishing/Reliability | „Atomisches“ Publish läuft mit dem konfigurierten Treiber nicht vollständig transaktional | `apps/renderer/src/app/admin/actions/publish.ts:105-180,221-275`; `packages/db/src/index.ts:1-8`; `apps/renderer/src/app/api/v1/content/publish/route.ts:51-115` | `createDb` verwendet immer `neon-http`; Admin läuft deshalb erwartbar durch den expliziten nicht-transaktionalen Fallback. Page-Status, Snapshot-Switch und History sind nicht gemeinsam atomar. Der PAT-Pfad implementiert einen zweiten sequenziellen Ablauf. | Partial State: Pages als published ohne neue Version, aktive Version ohne History oder falsches Nutzerfeedback. Ein Retry nach erfolgreichem Switch/fehlender History repariert History wegen Checksum-Deduplizierung nicht zwingend. | Eine Publish-Domainfunktion; Treiber/gespeicherte DB-Operation mit echter Atomizität oder explizite idempotente State-Machine. Outbox nur für Post-Commit-Nebenwirkungen. Page-Status nicht vor erfolgreichem Snapshot ändern. | Fehler injizieren nach jedem Schritt; vorheriger Snapshot bleibt aktiv, History/Status konsistent, Retry mit Idempotency-Key repariert/dupliziert nichts. |
| F-006 | Bestätigt | Tenant isolation | Reservierungsstatus-Update ohne Tenant-Predicate | `admin/functions/reservations/actions.ts:20-23`; `packages/db/src/schema/index.ts:436-450` | Action löst Tenant aus Session, verwendet ihn im Update aber nicht: `.where(eq(reservations.id,id))`. `status` wird zusätzlich als freier String akzeptiert. | Authentifizierter Tenant kann bei Kenntnis/Leak einer UUID fremde Reservierung verändern; ungültige Statuswerte können die State Machine beschädigen. | `and(id, tenantId)`, `returning()` und exakt eine betroffene Zeile; Status serverseitig als Enum/Allowlist; writable Capability verlangen. | Tenant A versucht ID von B → 404/0 Rows; Daten B unverändert; ungültiger Status → Validation Error; Demo → 403. |
| F-007 | Bestätigt | Design system/Renderer parity | Demo-Shell ignoriert Tenant-Design/Fonts; Surface-Rollen kollabieren | `demo/demo-page-shell.tsx`; `demo/[industry]/[[...slug]]/page.tsx`; `lib/brand-colors.ts:197-202`; `components/section-renderer.tsx:247-283`; [Extreme Matrix](evidence/section-extreme-matrix-summary.json) | Live Handwerk nutzt Outfit/Slate statt seed-konfiguriertem Fraunces/Inter/Creme. `sectionBgAlt ?? sectionBg ?? '#f8fafc'` macht Alt-Fallback unerreichbar; Aliases kreuzen Base/Alt. Timeline-Rail ist weiß auf weiß. Zusätzlich liefern 215/271 Renderer-Varianten im **expliziten Dark-Palette-Stresstest** mindestens ein rechnerisches Solid-Background-Kontrastsignal. Diese 215 Signale sind kein Beleg für 215 Production-Bugs und kein derzeit zugesagter Dark Mode, sondern zeigen, dass semantische Rollen unter einer alternativen Palette nicht robust genug getrennt sind. | Tenants wirken wie Palette-Swaps; Farbsteuerung lügt; konkrete Live-Bugs und ein systemischer Token-Stress-Risikoindikator. | Eine kanonische Theme-Funktion für Public/Demo/Preview/Collection/Shop: Style→Brand→Font→Design; Surface-Rollen nie cross-aliasen; Divider eigener Slot. Dark Mode entweder explizit nicht unterstützen oder als eigener vollständig geprüfter semantischer Modus definieren. | Computed-style Tests für Font, section-bg/alt/divider; Demo und Public Snapshot parity; Timeline-Kontrast; Contrast-Gate je **unterstütztem** Palettenmodus statt ungeprüfter Farbkombinationen. |
| F-008 | Bestätigt | Responsive/UI | Header bricht und erzeugt schwarze Compositor-Blöcke | `components/site-header.tsx`; Handwerk `/referenzen`, `/news` | Desktop-Textfragmente; bei 768 bleibt Desktopnav, bei 390 Logo/Menü abgeschnitten. Scroll-Richtungswechsel erzeugt 1,5–2,5 s schwarze Rechtecke. [Header](evidence/handwerk-header-referenzen-1440.png), [Compositor](evidence/handwerk-compositor-blocks-1140.png). | Navigation/Content zeitweise unbenutzbar, besonders mobile; Premium-Eindruck bricht. | Breakpoint aus Inhaltsbreite ableiten; Logo min-width/truncate; opaker stabiler scrolled Header, Blur/transform-Compositor reduzieren; Header-Visualtests. | 390/768/1140/1440; Scroll-Repro; kein Clip/Overlay/Layout shift. |
| F-009 | Bestätigt | Accessibility/Conversion | Sekundäre On-media-CTAs nahezu unsichtbar | Photography und Retail Hero; Hero/Token-Verträge | Live QA: [Photography](evidence/photography-secondary-cta-1440.png), [Retail](evidence/retail-secondary-cta-1440.png). Foreground/Border wird gegen Basissurface, nicht composited Image geprüft. | Wichtige Conversion-Aktion ist visuell nicht wahrnehmbar, WCAG-/Sales-Risiko. | Semantisches `secondary-on-media`-Paar und Contrast-Gate gegen Overlay+Media; Safe Fallback. | Screenshot + computed contrast für helle/dunkle Bilder und alle CTA-States. |
| F-010 | Bestätigt | CI/Schema drift | `main` besteht eigene Section-Gates nicht | `section-color-contracts-generated.ts`, `section-editor-field-defaults.ts` | `pnpm check:section-colors` → drift; `pnpm check:section-surface` → out of sync. Color-Check meldet paradoxerweise 0 added/removed/changed und Exit 1. | CI kann rot sein; Editor kann falsche/no-op Felder zeigen; Entwickler verlieren Vertrauen in Gates. | Generatoren reproduzierbar machen, Outputs neu erzeugen, Diff-Reporting reparieren, Gates vor Merge zwingend. | Clean checkout: beide Checks zweimal deterministisch grün; Mutationsfixture erzeugt lesbaren Diff. |
| F-011 | Bestätigt | Editor/Data loss | Optimistische Add/Delete/Reorder-Flows melden Erfolg ohne Rollback | `page-editor.tsx:345-420`; `pages/actions.ts:469-486` | Delete entfernt UI vor Request und zeigt danach immer Erfolg; Action-Fehler wird nicht behandelt. Reorder ist `Promise.all` einzelner Updates ohne Transaktion. | UI kann vom DB-Zustand abweichen; partielle Reihenfolge/Datenverlust; Nutzer glaubt fälschlich an Erfolg. | Mutation result/affected row zurückgeben, rollback/refetch bei Fehler; atomare Reorder-Action; Undo-Tombstone. | Fehler beim n-ten Reorder/Delete → UI/DB unverändert oder sauber zurückgerollt; Toast zeigt Fehler. |
| F-012 | Bestätigt | QA | Browser-/Visual-Regressions laufen nicht in CI | `.github/workflows/ci.yml`; `tests/visual-demos.spec.ts:17`; keine PNG-Baselines | CI baut Storybook, startet aber Playwright nicht. Visual-Test ist ohne `VISUAL_REGRESSION=1` geskippt; keine Baselines im Repo. | Genau die gemeldeten Header/Timeline/CTA/Mobile-Bugs passieren trotz grüner Unit-/Build-Checks. | Tier-1 Visual/interaction suite für zentrale Sections/Tenants/Admin in CI, Review-Artefakte und deterministische Fonts/Animations. | PR mit absichtlicher Rail-/CTA-Regression muss CI rot machen. |
| F-035 | Bestätigt | Provisioning/Reliability | Teilweise fehlgeschlagenes Provisioning wird trotzdem aktiv | `apps/marketing/src/lib/provisioning.ts:71-98,166-213,215-300`; `apps/marketing/src/lib/vercel.ts:90-235,239-304` | DB- und externe Vercel-/Blob-/Domain-Schritte laufen sequenziell. Standalone-Fehler werden nur als Warning zurückgegeben; bei `shared`/`lead_shared` werden Domainfehler geschluckt und `domainConfigured` trotzdem auf wahr gesetzt (`provisioning.ts:252-276`). Zeile 279–285 aktiviert den Tenant unabhängig davon. Retry löscht einen `provisioning`-DB-Tenant, kompensiert aber keine bereits erzeugten externen Ressourcen. Projekt-, Env- und Domainpfade sind teilweise idempotent, der Gesamtprozess ist es nicht nachweislich. | Kunde erhält aktiven, aber nicht erreichbaren/unvollständigen Tenant; verwaiste oder ungetrackte Side Effects, wiederholte Deployments, Ownership-Lücken und Kosten sind möglich. | Idempotente Provisioning-Saga mit persistenten Step States, Required Health Contract, Compensations und Aktivierung erst nach erfolgreichem Pflichtumfang; externe IDs unmittelbar speichern. | Fehler nach jedem DB-/Vercel-/Blob-/Domain-Schritt injizieren; Tenant bleibt provisioning/failed, Retry setzt gezielt fort, erzeugt keine ungetrackten Side Effects und Compensation löscht nur eigene Ressourcen. |
| F-036 | Bestätigt | Section responsive/Accessibility | `testimonialMarquee` läuft bei Tablet horizontal aus und ignoriert Reduced Motion | `apps/renderer/src/templates/shared/testimonial-marquee.tsx`; 1.024-px Runtime Matrix | Zwei unabhängige Läufe messen 56 px Body-Overflow; zwei CSS-Animationen bleiben bei `prefers-reduced-motion: reduce` aktiv. [Screenshot](evidence/section-testimonial-marquee.png). | Horizontaler Seitenscroll, verdeckter Inhalt und vestibuläre Barriere in einer Social-Proof-Section. | Marquee im Container clippen ohne Bodyoverflow; statischer/steuerbarer Fallback bei reduced motion; Pause/Controls/Keyboard; echte 1.024/390 Baselines. | 1.024/390 Body-ScrollWidth = Viewport; reduced motion → 0 kontinuierliche Animation; Tastatur/SR können alle Quotes erreichen. |
| F-037 | Bestätigt | Section reliability | `availabilityCalendar` erzeugt reproduzierbaren React-Hydration-Fehler | `apps/renderer/src/templates/shared/booking-widget.tsx`; Live `/section-preview?type=availabilityCalendar` | Zwei unabhängige Läufe erzeugen React Error #418 als `pageerror`, obwohl die Section sichtbar bleibt. [Screenshot](evidence/section-availability-calendar.png). | Instabile Interaktivität/Recovery in einer umsatzrelevanten Booking-Section; künftige React-/Datenänderung kann hart brechen. | Server-/Client-Date-/Locale-Ausgabe deterministisch machen; browserabhängige Werte erst nach Mount; Hydration-Regression mit fixer Uhr/Timezone. | 20 Wiederholungen in UTC/Europe-Berlin und 390/1.440 ohne Console/Page Error; Slots/Datum identisch hydriert. |

## P2 — Mittel

| ID | Confidence | Kategorie | Befund | Betroffene Stellen | Reproduktion/Evidence | Auswirkung | Lösung / Test |
|---|---|---|---|---|---|---|---|
| F-013 | Bestätigt | Content model | Seitenlöschung prüft Navigation/Inbound-Links nicht | `pages/actions.ts:116-121`; Navigation als JSON in `navigation` | Page wird direkt tenantgescoped gelöscht; kein Referenzscan/Redirect/Undo. | Tote Navigation, SEO-Verlust, Supportfälle. | Impact-Dialog, Link-Graph, Redirect/Gone-Option, Soft-delete; E2E „delete referenced page“. |
| F-014 | Bestätigt | CRUD UX | Slug-Kollision beim Erstellen/Umbenennen nicht nutzerfreundlich behandelt | `pages/actions.ts:99-113,206-212`; DB Unique `pages_tenant_slug_idx` | Zwei gleiche Titel erzeugen denselben Slug; Insert/Update lässt DB-Fehler hochlaufen. Duplicate-Action hat dagegen `uniqueSlug`. | Kernflow endet in generischem Fehler statt Vorschlag. | Einheitlicher Slug-Service, reservierte Routen, `-2`-Vorschlag, inline validation; Tests für Umlaute/leer/reserviert/race. |
| F-015 | Bestätigt | Performance/Reliability | Media-Library mutiert Daten beim Lesen und macht O(n) HEAD-Probes | `admin/media-actions.ts:62-145` | `getMediaAssets()` dedupliziert/updated/deleted und probt jedes Asset mit 2,5-s Timeout. | Langsame/instabile Media-Seite, Read kann Daten löschen; große Libraries skalieren schlecht. | Read side-effect-free; Dedupe/health als Job; Pagination; bounded concurrency/cache. Test 1k Assets, Provider-Timeout. |
| F-016 | Bestätigt | Abuse protection | Rate-Limit nur pro Serverless-Instanz | `lib/rate-limit.ts`; Login/Forms/Demo | Code dokumentiert selbst, dass Cold Starts/Bursts nicht vollständig gebremst werden. | Brute Force/Spam kann horizontal verteilt werden. | Redis/Upstash/DB-backed limiter, zusammengesetzte Keys, Monitoring. Distributed concurrency test. |
| F-017 | Bestätigt | API security | PAT ist tenantweit voll privilegiert und ungedrosselt | `lib/pat-auth.ts`; `/api/v1/**` | Hash/Ablauf/Widerruf gut, aber keine Scopes wie read/draft/publish/shop/media und kein PAT-Rate-Limit. | Leak/Fehlprompt kann gesamten Tenant verändern/veröffentlichen. | Scopes, per-token quotas, dry-run, max batch, audit log, optional approval for publish/delete. Contract tests. |
| F-018 | Bestätigt | SEO/Deployment | Demo `robots.txt` verweist Sitemap auf localhost | `apps/renderer/src/app/robots.ts`/Runtime | Live QA sah `https://localhost:3002/sitemap.xml`. | Crawler findet Sitemap nicht; Showcase wirkt unfertig. | Origin aus verifizierter Public Base URL/Request Host; production route test. |
| F-019 | Bestätigt | Showcase responsive | Section Showcase startet mobil mit 1280er Desktop-Preview | `demo/showcase/section-showcase-client.tsx` | 390px: Headline/Facts abgeschnitten, Preview kann overflowen. [Screenshot](evidence/showcase-wrong-default-390.png). | Mobile Evaluation unbrauchbar. | Viewport automatisch mobil, scale-to-fit, Auswahl merken; 390 screenshot/overflow test. |
| F-020 | Bestätigt | Overlay UX | Persistente Demo-/Consent-Steuerungen überlagern Mobile-Content | `demo-fab.tsx`, Cookie/Consent UI; Handwerk/alle Demos | Palette-FAB liegt über FAQ/Artikel/Bilder; Consent-Control dauerhaft 48px. | Touchziele verdeckt, Content nicht lesbar. | Mobile Demo-Toolbar statisch vor Header oder auto-hide; Safe Area/Content reserve; overlap tests. |
| F-021 | Bestätigt | Demo content/Routing | Handwerk-Bilder, Jahreszahlen und News-Routing sind inkonsistent | `scripts/demo-tenants/handwerk.cjs`; Demo Collection routes | Schlafzimmer als Leistung, Curology-Tube im Magazin, generische Immobilienbilder; 2025-Copy in 2026; Artikel verlässt `/demo/handwerk`. | Showcase beweist nicht Systemqualität; Vertrauensverlust. | Kuratierte Medienliste/Content lint; demo-aware Link Prefix; Datum/Alter dynamisch oder jährlich geprüft. |
| F-022 | Bestätigt | Product/UI | 15/17 Portfolio-Demos sind primär Palette-Swaps und überlang | Separate Live-Stichprobe der 17 Tenants ohne Handwerk; Handwerk eigener Deep Dive; offizielle Smokes decken alle 18 | Gleiche Kette Hero→Stats→Split→Cards→Process→Timeline→Testimonials→FAQ→CTA. Bis 13.433px Desktop/20.144px Mobile. | Branchenpositionierung ohne echte Experience-Differenz; geringe Conversion/Retention. | 6 Experience-Familien, 7–10 Kernsections, Details auf Unterseiten; intent-based recipes. Visual similarity/length budget. |
| F-023 | Bestätigt | Audit tooling | Registry-Audit zählt 28 `*Verein`-Keys als Sections | `scripts/audit-section-registry.ts:140-146` | Suffixregex entfernt viele Industries, nicht `Verein`; Tool meldet 250 statt 222 kanonische Typen. | Falsche Architektur-/Coverage-Metrik; „0 issues“ ist irreführend. | Definition Registry direkt importieren oder vollständige Ownerliste; invariant `reported types == unique definition types`. |
| F-024 | Bestätigt | Architecture/Observability | `draft_states`, `routes`, `audit_log` sind Schema ohne aktive Produktnutzung | `packages/db/src/schema/index.ts:182-223,363-392`; keine App-Referenzen | `rg` findet die Tabellen außerhalb Schema/Migration nicht. | Kein belastbarer Activity-/Recovery-/Redirect-Workflow trotz Datenmodellversprechen. | Implementierungsentscheidung dokumentieren; Audit-Log für kritische Mutationen zuerst; Dead schema später entfernen/migrieren. |
| F-025 | Bestätigt | Editor mental model | Sofortige und gestagte Saves sind vermischt | `page-editor.tsx:305-420,581-605`; Color/meta Actions | Reorder/Add/Delete speichern sofort, Text bleibt pending, Farben speichern debounced separat; dennoch ein globaler „Speichern“-Zustand. | „Ist das gespeichert/live?“ bleibt unklar; Race-/Recovery-Risiko. | Ein Draft-Command-Model mit einheitlichem Save-Status, per-operation activity log, queued mutations und publish diff. UX tests. |
| F-026 | Hohe Sicherheit | Upload hardening | API-Upload vertraut MIME/Extension, nicht Magic Bytes | `api/v1/content/upload/route.ts:10-43,46+` | Type/Extension allowlist und 10 MB Limit vorhanden; Bytesignatur/Decoding fehlt. | Falsch deklarierte/polyglotte Dateien im öffentlichen Blob Store; Provider-Header müssen Schutz übernehmen. | Magic-byte/decoder validation, strip metadata/re-encode, SVG weiterhin ablehnen; fixture tests. |
| F-027 | Möglich | Payment hardening | SumUp-Paid-Verifikation prüft Status, aber nicht Betrag/Währung/Reference | `api/shop/webhook/sumup/route.ts:41-64` | Im Gegensatz zu PayPal werden Amount/Currency/Reference nicht verglichen. Voller Exploit hängt vom Checkout-Erstellpfad/Providervertrag ab. | Mögliches falsches Zuordnen eines bezahlten Checkouts. | Providerantwort gegen orderId/amount/currency prüfen; Live-Sandbox E2E, Replay/Idempotency. |
| F-028 | Bestätigt | Performance | Hohe Client-Bundles in Kernflows | Renderer Production Build | Public Tenant-Seiten ca. 495–500 kB First Load JS; Page Editor 818 kB; Section-Lab Chunk 1,75 MB. | Langsamer Start/Interaktion auf schwachen Mobilgeräten; Editor wirkt schwer. | Template lazy registry, route-level/editor code-splitting, Motion/Icons on demand, bundle budget. Lighthouse/Web Vitals. |
| F-029 | Bestätigt | Docs/CI | README und Admin-Build spiegeln Architektur nicht | `README.md`; `.github/workflows/ci.yml` | README sagt Admin/Renderer „coming soon“; `@flamingo/admin`-Build meldet „No projects matched“ mit Exit 0. | Onboarding-/Betriebsfehler, falsches Sicherheitsgefühl. | README/CI auf integrierten Renderer korrigieren; CI soll bei unbekanntem Filter scheitern. |
| F-030 | Bestätigt | Variant strategy | Style-Auswahl ist praktisch auf `classic` reduziert | `admin/settings-actions.ts:227-230`; Demo-/Tenant Seeds | Setter schreibt unabhängig von Eingabe `activeStyle: 'classic'`; alle Demo-Skripte nutzen Classic. | Variantenversprechen und Branchendifferenzierung greifen kaum. | Varianten entweder bewusst entfernen/als Recipe modellieren oder vollständig token-/component-kompatibel implementieren und testen. |
| F-038 | Bestätigt | Section accessibility | Mehrere Sections besitzen strukturelle A11y-/Motion-Signale | [Runtime Matrix](evidence/section-runtime-matrix-summary.json); [Extreme Matrix](evidence/section-extreme-matrix-summary.json) | Targeted Axe bestätigt kritische Name-/Label-Verstöße in `headerBanner` (1 namenloser Button), `priceCalculator` (2 namenlose Buttons; zusätzlich 13 serious Contrast-Nodes), `rsvp` (1 nicht zugeordnetes Guest-Count-Label) und `shopProductGrid` (2 Range-Inputs ohne Label plus 1 Select ohne Namen). Source Review zeigt zusätzlich unbenannte Close/Previous/Next-Controls im geöffneten Gallery-Lightbox-State. Die Production-Route meldet sechs Typen mit Heading-Level-Sprung und zehn Typen mit **kontinuierlicher** CSS-Animation trotz Reduced Motion. Im breiteren Storybook-Stresstest zeigen 95/271 Varianten unmittelbar nach Load noch Animation; darin sind auch endliche Entrance-Animationen enthalten. Positiv: vollständige Tab-Traversals über 271/271 Varianten fanden 0 unerreichbare Controls und jeweils einen sichtbaren Fokusindikator. | Screen-reader-Verständnis und Motion-Präferenz sind nicht systemweit zuverlässig; die positive Tab-Abdeckung entkräftet die konkreten Name-/Label- und Motion-Befunde nicht. | Primitive-Level Name/Label/Heading/Motion Contract; Lightbox-Controls explizit benennen; Inputs korrekt über `label`/`aria-*` verknüpfen; Playwright ARIA Snapshots; Storybook A11y `error`; Akzeptanz: Axe serious/critical = 0, 0 namenlose Controls/Heading-Sprünge und 0 kontinuierliche Animation bei Reduce. Manuelle SR-Prüfung bleibt Pflicht. |
| F-039 | Bestätigt | Fixture/Content resilience | `faqGallery` rendert mit gebrochenem Preview-Bild und ohne API-Schema | `apps/renderer/src/templates/wedding/faq-gallery.tsx`; Preview Fixture; [Screenshot](evidence/section-faq-gallery.png) | Vier Viewports zeigen ein Asset mit `naturalWidth=0`; Typ ist renderbar, aber weder im Picker noch im API-Schema. | Section-Lab/Quality-Audit kann falsche Ergebnisse liefern; Legacy Content hat keinen validierten Fallback. | Fixture reparieren; Typ entfernen/aliasieren oder Schema/Editor/Fallback ergänzen; Test: 0 kaputte Assets, Registry/Picker/API-Entscheidung explizit, Missing-media Baseline. |
| F-040 | Bestätigt | Section content resilience | Realistisch erweiterter Inhalt erzeugt horizontalen Overflow in acht benannten Renderer-Vertragsfamilien | `apps/renderer/src/stories/section-audit.stories.tsx`; [Extreme Matrix](evidence/section-extreme-matrix-summary.json) | 3.794 Storybook-Zustände über 271 sinnvolle Renderer-Varianten finden unter Odd-/Many-/Long-/Zoom-Stress: `beforeAfterStoryPro` 56 px, `ctaLinks` 23 px, `headerBanner` bis 715 px, `pricingInfo` 45 px, `proofWall` 112 px, `story.hotel` 157 px/`story.restaurant` 62 px, `testimonialMarquee.ecommerce` 56 px Tablet bzw. 112 px Zoom und `transformationStories` 56 px. `beforeAfterStoryPro` und `transformationStories` teilen eine Implementierung, bleiben aber getrennte Vertragsfamilien. Nur `testimonialMarquee` ist zusätzlich im normalen Production-Fixture bei 1.024 px mit 56 px bestätigt; die übrigen Werte sind Resilience-Fehler unter realistischen erweiterten Stressdaten, keine Behauptung über jeden Default-Tenant. | Kundentexte, Übersetzungen und größere Datensätze können Seitenscroll, abgeschnittene Inhalte oder nicht bedienbare CTAs erzeugen; `headerBanner` ist besonders fragil. | Intrinsische Größen (`min-width:0`, Wrap/Grid-Minmax), keine viewportbreiten inneren Rails, explizite Contentbudgets und kontrolliertes Clamp/Expand; Zoom und Odd/Many als Contract. **Test:** Für alle acht Familien 390/430/1.024/1.440, Odd/Many/Long und nativer 200-%-Zoom: `scrollWidth <= clientWidth`, kein Clip/Overlap; Production-Smoke für `testimonialMarquee`. |
| F-041 | Bestätigt | Section media resilience | Missing-Media-State erzeugt in 13 Renderer-Varianten kaputte `<img>`-Elemente | `apps/renderer/src/stories/section-audit.stories.tsx`; [Extreme Matrix](evidence/section-extreme-matrix-summary.json) | Der explizite Missing-Media-Stress reproduziert Broken Images in `agentTeam`, `atmosphereGallery`, `beforeAfterSlider`, `faqGallery`, `gallery`, `galleryGrid`, `galleryMoodboard`, `galleryPro`, `horizontalScrollShowcase`, `logoCloud`, `portfolio`, `portfolioGallery` und `referencesSold`. Das beweist das Fallback-Verhalten, nicht dass alle Default-/Live-Daten bereits kaputt sind. Bei vertraglich erforderlichem Medium ist dieser Zustand **ungültige Eingabe** und muss vor Persist/Publish abgewiesen werden; bei optionalem Medium ist Abwesenheit gültig und darf kein kaputtes Bild erzeugen. | Gelöschte Assets, AI-/Importlücken oder optionale Felder erzeugen sichtbare Browserfehler, Layoutlöcher und unprofessionelle Demos. | Required Media in Schema/Preflight hart validieren; optionale Medien semantisch auslassen oder mit intentionalem, alt-korrektem Fallback darstellen; Asset-Health/Usage-Graph für nachträgliche Löschung. **Test:** Required fehlt/defekt → Validation Error vor Publish; optional fehlt → kein requestendes `<img>`/kein Broken-Asset; Provider-404 und gelöschtes Asset in allen 13 Varianten testen. |
| F-031 | Bestätigt | Section UI/UX | FAQ erzeugt tote Fläche; Timeline verliert sichtbare Achse und Rhythmus | FAQ-/Timeline-Renderer und Token-Surface; [FAQ](evidence/faq-desktop-1440.png), [Timeline](evidence/timeline-desktop-1440.png) | Desktop/Mobile und isolierte Preview reproduziert; Timeline-Verbindung ist kaum sichtbar, FAQ-Section deutlich höher als ihr Inhalt. | Unnötige Scrollstrecke, schlechtere Narration und sichtbar unfertiger Premium-Eindruck. | Erzwungene Höhe entfernen; Rail eigener Contrast Slot; kompakter Editorial Rhythm; Test: 390/768/1.440, 1/2/odd/many, sichtbare Rail, keine tote Restfläche. |
| F-032 | Bestätigt | Responsive UI | Hotel-Dekoration überlagert Mobile-Copy | Hotel Hero; [Screenshot](evidence/hotel-decor-overlap-390.png) | Goldener Stern liegt bei 390 px über einem Wort der Subline. | Lesbarkeit und Premium-Eindruck brechen im wichtigsten Einstieg. | Text-Safe-Zone; Deko unter 768 ausblenden/flowbinden; Test: 320/390/430 mit Long-copy ohne Text-/CTA-Überschneidung. |

## P3 — Niedrig/Polish

| ID | Confidence | Befund | Evidence | Empfehlung |
|---|---|---|---|---|
| F-033 | Bestätigt | Admin-Fehlertext ist teils englischer Next-Standardtext; Tour und Cookie-Banner konkurrieren | Admin Runtime Screenshots | Nutzerfreundliche Fehler-ID/Recovery; Consent vor Tour oder im Admin unterdrücken. |

## Zehn wichtigste fehlende Regressionstests

1. Unauthentifizierter Marketing-Mailversand ist unmöglich.
2. Demo-Rolle kann keine irreversiblen oder tenantglobalen Daten ändern.
3. Snapshot-Read-Fehler darf nie Draft rendern.
4. Zwei parallele Publish-Versuche erzeugen genau einen konsistent aktiven Snapshot.
5. Tenant A kann keine Reservation/Media/Page/Order von B mutieren.
6. Admin Demo Login → Dashboard → Pages → Editor → Save → Preview funktioniert.
7. Add/Delete/Reorder schlägt kontrolliert fehl und rollt UI/DB zurück.
8. Demo/Public/Preview liefern identische Fonts und Design-/Surface-Tokens.
9. Alle 222 Section-Typen/271 sinnvollen Renderer-Varianten bei 1440/1024/430/390, reduced motion, Zoom, langen sowie Missing-Media-Inhalten als reviewbare Visual-Baseline.
10. Payment-Provider: Signatur, Amount/Currency, Replay und Statusübergänge.

## Vollständige risikobasierte Testmatrix

`P0/P1/P2` in dieser Tabelle beschreibt den Geschäftsschaden eines unentdeckten Fehlers, nicht den aktuellen Teststatus.

| Domäne / kritischer Vertrag | Aktuelle Evidenz | Hauptlücke | Empfohlene Testebenen | Risiko | Acceptance Gate |
|---|---|---|---|---|---|
| Tenant-Auflösung | Unit-/Crosstalk-Tests; statischer Codepfad | Host/slug/fixed/local Kombinationen und Cache-Grenzen nicht vollständig | table-driven unit + zwei Deployment-Integrationstests | P0 | jeder Host/Slug löst ausschließlich erwarteten aktiven Tenant; kein Local-Fallback in Production |
| Tenant-Isolation Reads | viele tenantgefilterte Queries; einzelne Tests | kein schemaweites Query-/Repository-Gate | zwei-Tenant-Integration je Repository/Action; mutation testing | P0 | fremde UUID/Slug liefert 404/leer ohne Existenzleck |
| Tenant-Isolation Writes | Reservation-Lücke bestätigt | keine vollständige Mutation-Matrix | zwei-Tenant-Integration für jede Mutation; affected-row assert | P0 | kein Write ohne `tenantId`; genau eine erwartete Zeile betroffen |
| Login/Session | Auth-Unit-Tests, bcrypt/JWT review | Rotation, Revocation, distributed brute force, fixation | Auth-Integration + Cookie-Browser + distributed rate-limit | P0 | invalid/expired/rotated token wirkungslos; sichere Cookies; Rate Limit instanzübergreifend |
| Authorization/Capabilities | `getWritableSession` für einzelne Flows | Demo nutzt viele `getSession`-Writes; keine Rollenmatrix | generiertes Route-/Action-Inventar × Rollen/Capabilities | P0 | default-deny; Demo erzeugt DB-/Blob-/Mail-/Secret-Diff 0 |
| Page Create | Action coverage teilweise | Duplicate Name/Slug, reserved routes, leere Titel | DB integration + browser journey | P1 | deterministische freie URL oder Inline-Fehler; keine 500/Orphans |
| Page Rename/Slug | DB Unique Constraint | Kollision, Redirect, Navigation/Canonical-Auswirkung | Integration + Link Graph + Browser | P1 | atomarer Rename, Redirect/Navigation konsistent, alte URL kontrolliert |
| Page Duplicate/Delete | Duplicate erzeugt unique slug | Navigation-Referenzen, Redirect, Orphans, Recovery | Integration + dependency graph + undo | P1 | Impact Preview; kein Broken Link; Soft-delete/Undo oder bestätigte Cascade |
| Section Add/Clone/Delete | Registry-/Schema-Tests | optimistic failure/rollback, legacy identity | Action integration + network-failure browser | P1 | Fehler lässt UI/DB identisch; Definition-Key/Version korrekt; Undo möglich |
| Section Reorder/Visibility | einfache Action-Tests | N Einzelwrites, Race, keyboard alternative | transactional/concurrency integration + keyboard browser | P1 | Reihenfolge atomar/eindeutig; stale revision abgewiesen; Tastaturalternative |
| Section Schema Compatibility | Registry-/Contract-Audits | 5 renderbare Typen ohne API-Schema; Legacymigration | generated parity + fixture je schemaVersion | P1 | Picker/API/Editor/Renderer stimmen überein; alte Version migriert/klar abgewiesen |
| Draft/Public Isolation | Snapshot-Pfade geprüft | Error und no-snapshot lesen Draft | fault injection + Public-HTML-Assertion | P0 | Public enthält unter keinem Fehlerzustand Draft-Marker |
| Live Preview Parity | Preview-Komponenten vorhanden | Theme/Font/Data unterscheiden sich von Public | gleiche Snapshot-Fixture in Preview/Public + screenshot diff | P1 | gleiche Struktur, Tokens, Fonts und Links außer Edit-Overlays |
| Publish | Preflight-/Unitchecks | neon-http Sequenz nicht atomar; zwei Implementierungen | DB fault injection je Schritt + concurrency/idempotency | P0 | Fehler vor Commit ändert Live/Status/History nicht; Retry konsistent |
| Rollback/History | Funktion vorhanden | nichttransaktionaler Fallback, History-Recovery | version chain + fault injection + browser | P0 | exakt gewählte Version aktiv; vollständige History; Cache nachholbar |
| Media Upload | Type/size/overwrite tests teilweise | Magic bytes, re-encode, quota | hostile corpus + provider adapter integration | P1 | nur echte erlaubte Bilder; Tenantpfad/Quota; kein overwrite/polyglot |
| Media Replace/Delete | einzelne Actions | Usage Graph, Demo Delete, Read mutiert DB | reference graph + provider-failure + two-tenant | P0 | verwendetes Asset nicht still gelöscht; Retry/rollback; Demo 403 |
| Navigation/Footer | Quellreview | Page Delete/Rename hinterlässt JSON-Links | dependency integration + full-site link crawl | P1 | 0 interne Broken Links; locale-/global references konsistent |
| SEO/Routes | Live robots inspection | localhost Sitemap, Redirect/Canonical/JSON-LD | per-tenant crawl + schema validation | P1 | Production-Host, eindeutige Canonicals, valide strukturierte Daten |
| Global Design/Tokens | 5/5 Token Gates; zwei Section-Gates rot | Runtime-Parität, composited contrast, generated drift | property tests + computed styles + screenshot presets | P1 | alle Gates deterministisch grün; AA je semantischem Zustand |
| Alle Sections | 222 kanonische Typen; 271 sinnvolle `(type, component)`-Renderer-Varianten; 3.794 Storybook-Stresszustände; 0 Hard Failures | CI-Gate, manueller Screen Reader, nativer Browserzoom und realistischer fotografischer Medienkorpus | Registry build + Fixture-/Extreme-Matrix + Visual/A11y Gate | P1 | jede Variante normal/min/max/long/missing, alle unterstützten Paletten plus expliziter Dark-Stress, Reduced Motion und Zoom bei 390/430/1.024/1.440; keine stillen Skips |
| Responsive Visual | 832 Production-Route-Viewportzustände plus 3.794 Storybook-Zustände | nativer Browser-200-%-Zoom, reale Medien-/Übersetzungskorpora und committed reviewbare Baselines | Playwright Screenshots + Production-Smokes + Review-Gate | P1 | kein Overflow/Clip/Overlap; Contentbudget/Breakpoints erfüllt; Stressbefund explizit triagiert |
| Accessibility | vollständige Tab-Traversals 271/271: 0 unerreichbare Controls, sichtbarer Fokus; targeted Axe und Reduced-Motion-Scans | manueller SR, nativer 200-%-Zoom, geöffnete Dialog-/Carousel-/Lightbox-States vollständig | Axe + Keyboard + ARIA Snapshots + manuelle SR-Checkliste kritischer Journeys | P1 | 0 serious/critical Axe; Tastatur; 200 % Zoom; keine kontinuierliche Motion bei Reduce |
| Forms/Mail | Validation/Honeypot/Sanitizer tests | SMTP relay, distributed spam, retry, PII retention | Route Security + Mail Adapter + Abuse/Load | P0 | kein unauth Mail; idempotenter Submit; Recovery/Retention geprüft |
| Shop/Checkout | Payment code + einige Tests | Provider E2E, Amount/Reference, concurrent webhook | Stripe/PayPal/SumUp sandbox + replay/concurrency | P0 | Signatur/Tenant/Amount/Currency/Reference; ein Übergang je Event |
| Booking/Reservations | Kernlogik vorhanden | Tenant predicate, Status, DST/Overlap, retry | timezone/property + two-tenant/concurrency E2E | P0 | keine Überbuchung; DST korrekt; fremde UUID/Status abgewiesen |
| PAT/AI API | Auth-/Schema-Tests teilweise | Scopes/Quota, destructive apply, weak-model behavior | Contract/fuzz + scope + idempotency + eval corpus | P1 | discover/validate/dry-run/apply getrennt; Delete/Publish approval-gated |
| Migrations/Backfill | 17 Migrationen statisch geprüft | kein produktnahes fresh+upgrade CI | leere DB + N-1 fixture + snapshot/backfill | P0 | fresh/upgrade identisch; Backfill idempotent; Rollbackplan |
| Provisioning/Domain | CRM/Vercel Workflow im Code | Partial Vercel/Blob/Domain failure, Zombie Cleanup | provider fakes + staged integration + compensation | P1 | Tenant erst vollständig aktiv; Retry ohne Duplikat; Secrets korrekt |
| Deployment/CI | Builds/Live-Smokes grün | Phantom-Admin-Build; Browser/Visual optional | clean-checkout CI + preview deploy + intentional break | P0 | echter Build, Migration, critical E2E und Gates blockieren Merge |
| Observability/Recovery | Logs/Error Boundaries | keine korrelierbaren SLO-/Alert-/Runbook-Tests | synthetic failures + alert delivery + restore drill | P1 | kritischer Fehler erzeugt Alert/Korrelation; Restore-Ziel belegt |
